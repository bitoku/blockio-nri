# NRI IOPS Plugin Configuration
#
# Default block device to apply limits to when the annotation
# does not specify a device path.
# defaultDevice: /dev/sda

# Whether to block container creation when the io-limits
# annotation is malformed. Default: false (log warning only).
failOnInvalidAnnotation: false

# Log level: debug, info, warn, error
logLevel: info
